---
phase: 03-replace-fixed-waits-with-smart-detection
plan: 04
type: execute
wave: 4
depends_on:
  - 03-02
files_modified:
  - tests/lib/workflows.js
  - tests/readiness-pacing.spec.js
autonomous: true
gap_closure: true
requirements:
  - PERF-02

must_haves:
  truths:
    - "The exact unconditional delay: 10000 in profile-to-home activation is removed only as the PERF-02 verification-gap exception mandated by the Phase 3 roadmap."
    - "waitForHomeReady() remains the state-driven completion boundary and still requires route, visible left menu, content rows, and valid focus."
    - "D-08 remains in force for every other explicit activation/transition delay: those delays remain unchanged, and the profile activation retains all existing selector contract fields."
    - "A named deterministic profile-to-home regression test is present in the readiness spec and is executed by the focused Playwright verification."
  artifacts:
    - path: tests/lib/workflows.js
      provides: "Profile-to-home flow with verified activation followed immediately by bounded home readiness"
      contains: "chooseFirstProfileAndEnterHome"
    - path: tests/readiness-pacing.spec.js
      provides: "Named static workflow-contract regression for PERF-02 plus existing synthetic home-readiness coverage"
      contains: "profile-to-home workflow contract removes the PERF-02 delay"
  key_links:
    - from: tests/lib/workflows.js
      to: tests/lib/waits.js
      via: "chooseFirstProfileAndEnterHome calls waitForHomeReady after verified profile activation"
      pattern: "activateVerifiedTarget[\\s\\S]*waitForHomeReady"
    - from: tests/readiness-pacing.spec.js
      to: tests/lib/workflows.js
      via: "named deterministic source-contract assertion reads and checks the active profile flow"
      pattern: "test\\(.*profile-to-home workflow contract removes the PERF-02 delay[\\s\\S]*readFileSync.*workflows"
---

<objective>
Close the PERF-02 verification gap by removing the exact unconditional `delay: 10000` profile-to-home delay as the narrow exception mandated by the Phase 3 roadmap, and add deterministic regression coverage for the exact workflow contract.

Purpose: make the existing bounded `waitForHomeReady()` predicate, not this named readiness-gap sleep, determine login completion while preserving verified profile activation semantics and every other D-08-protected explicit activation/transition delay.
Output: the focused workflow fix and regression assertions in `tests/readiness-pacing.spec.js`.
</objective>

<execution_context>
@/Users/thainguyen/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thainguyen/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-replace-fixed-waits-with-smart-detection/03-VERIFICATION.md
@.planning/phases/03-replace-fixed-waits-with-smart-detection/03-CONTEXT.md
@tests/lib/workflows.js
@tests/readiness-pacing.spec.js
@tests/lib/selector-validation.js

The verification gap is limited to `chooseFirstProfileAndEnterHome()`: its `activateVerifiedTarget()` call passes the exact `delay: 10000` before `waitForHomeReady()`. The Phase 3 roadmap requires removing this exact delay to close PERF-02; this is the only delay-removal exception in this plan. D-02 and D-03 require home route, visible menu, content, and focus readiness through a bounded wait. D-08 still requires every other explicitly supplied activation/transition delay to remain unchanged. Preserve all profile selector contract fields (`testInfo`, `name: "profile-selection"`, `contractName: "contentItem"`, and `expectedId: "item_0"`). Keep this plan limited to PERF-02; do not modify `app/`, existing plans, or unrelated legacy waits.
</context>

<tasks>

<task type="auto">
  <name>Remove the profile-to-home fixed delay</name>
  <files>tests/lib/workflows.js</files>
  <action>
    In `chooseFirstProfileAndEnterHome(page, testInfo)`, remove only the exact unconditional `delay: 10000` option from the `activateVerifiedTarget()` call as the PERF-02 verification-gap exception mandated by the Phase 3 roadmap. This is not a general relaxation of D-08: preserve every other explicit activation/transition delay in `tests/lib/workflows.js`. Preserve every existing profile selector contract field — `testInfo`, `name: "profile-selection"`, `contractName: "contentItem"`, and `expectedId: "item_0"` — so verified keyboard activation and selector diagnostics keep the existing contract. Leave the immediately following `await waitForHomeReady(page, testInfo)` as the completion boundary. Do not modify `app/`, any other production code, or unrelated legacy waits.
  </action>
  <verify>
    <automated>node --check tests/lib/workflows.js</automated>
    <automated>node -e 'const fs=require("fs"); const s=fs.readFileSync("tests/lib/workflows.js","utf8"); const start=s.indexOf("async function chooseFirstProfileAndEnterHome"); const end=s.indexOf("async function closeHomePopupsAndVerifyHome"); const body=s.slice(start,end); if (/delay\s*:\s*10000/.test(body)) process.exit(1); for (const field of ["testInfo", "name: \"profile-selection\"", "contractName: \"contentItem\"", "expectedId: \"item_0\""]) if (!body.includes(field)) process.exit(1); if (body.indexOf("waitForHomeReady(page, testInfo)") < 0) process.exit(1); const outside=s.slice(0,start)+s.slice(end); const delays=[...outside.matchAll(/delay\\s*:\\s*(\\d+)/g)].map((m)=>Number(m[1])); const expected=[2000,2000,1500,2000,5000,3000,3000,3000,2000,3000,2500,3000,6000,6000,6000,6000,2500,2500,2500]; if (JSON.stringify(delays)!==JSON.stringify(expected)) process.exit(1);'</automated>
  </verify>
  <done>`chooseFirstProfileAndEnterHome()` removes only the exact `delay: 10000` named by the PERF-02 roadmap gap, performs verified profile activation with all four existing selector contract fields, then invokes `waitForHomeReady(page, testInfo)`; every other explicit activation/transition delay remains unchanged and the module remains syntactically valid.</done>
</task>

<task type="auto">
  <name>Add deterministic PERF-02 workflow-contract regression</name>
  <files>tests/readiness-pacing.spec.js</files>
  <action>
    Add a named focused Playwright test with the exact title `profile-to-home workflow contract removes the PERF-02 delay`, using `fs.readFileSync()` to inspect only the `chooseFirstProfileAndEnterHome()` function body in `tests/lib/workflows.js`. Assert that profile activation still calls `activateVerifiedTarget()` with all existing contract fields — `testInfo`, the `profile-selection` name, `contentItem` contract, and `item_0` identity —; assert that the call contains no unconditional `delay: 10000`; and assert that `waitForHomeReady(page, testInfo)` follows the activation call. Also assert the ordered non-profile `activateVerifiedTarget()` delay literals remain unchanged so D-08's preservation boundary is executable. Keep the existing synthetic home-readiness tests intact so the regression proves both the narrow PERF-02 exception and the route/menu/content/focus readiness contract without APP_URL, credentials, or live staging. Do not alter `tests/selector-contracts.spec.js` or existing plans.
  </action>
  <verify>
    <automated>node --check tests/readiness-pacing.spec.js</automated>
    <automated>node -e 'const fs=require("fs"); const s=fs.readFileSync("tests/readiness-pacing.spec.js","utf8"); if (!s.includes("test(\"profile-to-home workflow contract removes the PERF-02 delay\"")) process.exit(1); if (!s.includes("readFileSync") || !s.includes("workflows.js")) process.exit(1);'</automated>
    <automated>npx playwright test tests/readiness-pacing.spec.js --grep "profile-to-home workflow contract removes the PERF-02 delay"</automated>
    <automated>npx playwright test tests/readiness-pacing.spec.js</automated>
  </verify>
  <done>The named `profile-to-home workflow contract removes the PERF-02 delay` test is present by source assertion and is explicitly executed by the focused Playwright command; it fails on reintroduction of the exact 10-second profile delay, removal of any profile activation contract field, or modification of other explicit activation/transition delays, while the full readiness spec continues to cover the existing smart home-readiness predicate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test source to deterministic regression | The spec reads repository source text; the file content is treated as the asserted local artifact, not as runtime application input. |
| Test workflow to MyTV page | The existing keyboard-only workflow sends activation to the external TV page; this gap fix adds no new input or network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | `tests/readiness-pacing.spec.js` source-contract assertion | mitigate | Require the named `profile-to-home workflow contract removes the PERF-02 delay` test, extract only the named function body, and assert the exact activation identity, selector contract, ordering, absence of `delay: 10000`, and unchanged non-profile delay literals. |
| T-03-04-02 | Elevation of privilege | Profile activation in `tests/lib/workflows.js` | accept | This plan preserves the existing `activateVerifiedTarget()` validation and keyboard-only Enter path; it introduces no authentication or authorization behavior. |
| T-03-04-SC | Tampering | npm/Playwright test execution | mitigate | No package installs are required; run syntax and the focused existing deterministic spec only. |
</threat_model>

<verification>
<automated>node --check tests/lib/workflows.js</automated>
<automated>node --check tests/readiness-pacing.spec.js</automated>
<automated>node -e 'const fs=require("fs"); const s=fs.readFileSync("tests/lib/workflows.js","utf8"); const start=s.indexOf("async function chooseFirstProfileAndEnterHome"); const end=s.indexOf("async function closeHomePopupsAndVerifyHome"); const body=s.slice(start,end); if (/delay\s*:\s*10000/.test(body) || body.indexOf("waitForHomeReady(page, testInfo)") < 0) process.exit(1); for (const field of ["testInfo", "name: \"profile-selection\"", "contractName: \"contentItem\"", "expectedId: \"item_0\""]) if (!body.includes(field)) process.exit(1); const outside=s.slice(0,start)+s.slice(end); const delays=[...outside.matchAll(/delay\\s*:\\s*(\\d+)/g)].map((m)=>Number(m[1])); const expected=[2000,2000,1500,2000,5000,3000,3000,3000,2000,3000,2500,3000,6000,6000,6000,6000,2500,2500,2500]; if (JSON.stringify(delays)!==JSON.stringify(expected)) process.exit(1);'</automated>
<automated>node -e 'const fs=require("fs"); const s=fs.readFileSync("tests/readiness-pacing.spec.js","utf8"); if (!s.includes("test(\"profile-to-home workflow contract removes the PERF-02 delay\"")) process.exit(1); if (!s.includes("readFileSync") || !s.includes("workflows.js")) process.exit(1);'</automated>
<automated>npx playwright test tests/readiness-pacing.spec.js --grep "profile-to-home workflow contract removes the PERF-02 delay"</automated>
<automated>npx playwright test tests/readiness-pacing.spec.js</automated>
</verification>

<success_criteria>
- The exact `delay: 10000` in `chooseFirstProfileAndEnterHome()` is removed solely as the PERF-02 verification-gap exception mandated by the Phase 3 roadmap; `waitForHomeReady()` remains the completion boundary.
- D-08 is accurately preserved: every other explicit activation/transition delay remains unchanged, and profile activation retains `testInfo`, `profile-selection`, `contentItem`, and `item_0` contract fields.
- The named `profile-to-home workflow contract removes the PERF-02 delay` regression exists by source contract and is explicitly included in the focused Playwright command; deterministic readiness coverage proves the narrow no-fixed-delay exception and explicit-activation contracts while preserving the existing home route/menu/content/focus checks.
- Only `tests/lib/workflows.js` and `tests/readiness-pacing.spec.js` are changed by execution; no production app code or existing plan is modified.
</success_criteria>

<output>
Create `.planning/phases/03-replace-fixed-waits-with-smart-detection/03-04-SUMMARY.md` when done.
</output>
