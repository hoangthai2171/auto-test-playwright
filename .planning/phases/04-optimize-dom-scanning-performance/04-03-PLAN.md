---
phase: 04-optimize-dom-scanning-performance
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - tests/lib/workflows.js
  - tests/lib/selector-validation.js
  - tests/locator-contracts.spec.js
autonomous: true
requirements:
  - PERF-09

must_haves:
  truths:
    - "D-08: Stable structural controls use scoped Playwright Locators and centralized selector contracts where the DOM is stable."
    - "D-09: Dynamic content discovery and fuzzy Vietnamese scoring remain evaluate-based while centralized selector contracts remain the source of truth for stable Locators."
    - "D-10: A Locator contract miss has one diagnosable evaluate fallback and is not silently swallowed."
    - "D-11: All actions remain keyboard-only: Locator lookup/filtering is followed by remote focus, selector validation, and Enter; no locator.click() is introduced."
  artifacts:
    - path: tests/lib/workflows.js
      provides: "Stable menu/settings/action lookup through contracts and Locator filtering"
      contains: "locator"
    - path: tests/locator-contracts.spec.js
      provides: "Deterministic Locator selection, fallback, and keyboard activation coverage"
      contains: "locator"
  key_links:
    - from: tests/lib/workflows.js
      to: tests/lib/selectors.js
      via: "stable Locator roots and filters use central contracts"
      pattern: "getSelectorContract|locator"
    - from: tests/lib/workflows.js
      to: tests/lib/selector-validation.js
      via: "Locator targets still pass verified activation before Enter"
      pattern: "activateVerifiedTarget"

---

<objective>
Adopt Playwright Locator filtering for stable structural elements without forcing dynamic content discovery into brittle selectors or changing the TV remote activation model.

Purpose: satisfy PERF-09 at the stable boundary while preserving fuzzy, geometry-aware discovery for dynamic MyTV content.
Output: contract-backed Locator helpers, bounded diagnostic fallback, and deterministic activation regressions.
</objective>

<decision_coverage>
- D-08: Use Locators only for stable structural elements; dynamic fuzzy content remains contract-aware and evaluate-based.
- D-09: Central selector contracts remain the source of truth for stable Locators.
- D-10: A Locator contract miss gets one diagnostic evaluate fallback and is not silently hidden.
- D-11: Locator lookup still ends in remote focus, selector validation, and keyboard Enter; locator.click() is forbidden.
</decision_coverage>

<execution_context>
@.planning/phases/04-optimize-dom-scanning-performance/04-CONTEXT.md
@.planning/phases/04-optimize-dom-scanning-performance/04-RESEARCH.md
@tests/lib/selectors.js
@tests/lib/workflows.js
@tests/lib/selector-validation.js
@tests/lib/navigation.js
</execution_context>

<tasks>

<task type="auto">
  <name>Add contract-backed stable Locator helpers</name>
  <files>tests/lib/workflows.js, tests/lib/selector-validation.js</files>
  <action>
    Add narrow helper functions for stable left-menu, settings/account, and known action containers using `page.locator()` plus contract-backed CSS/attribute roots and `.filter({hasText, has})` where appropriate. Keep dynamic content rows, service cards, search candidates, fuzzy Vietnamese scoring, and geometry-heavy enumeration on the scoped evaluate path from earlier plans. The helpers should return target metadata or a locator-backed candidate without clicking or retaining a live handle in snapshots.
  </action>
  <verify>
    <automated>node --check tests/lib/workflows.js</automated>
    <automated>node --check tests/lib/selector-validation.js</automated>
    <automated>node -e 'const w=require("./tests/lib/workflows"); if(!w.__internal) process.exit(1);'</automated>
  </verify>
  <done>Stable structural lookups use centralized contracts and Locator filtering, while dynamic content remains on the specialized scoped evaluator.</done>
</task>

<task type="auto">
  <name>Preserve diagnostic fallback and keyboard activation</name>
  <files>tests/lib/workflows.js, tests/lib/selector-validation.js</files>
  <action>
    Integrate the stable Locator helpers at left-menu/settings/action call sites without replacing `activateVerifiedTarget()`. On a missing or unusable Locator contract, invoke the existing evaluate-based discovery once, attach the established diagnostic evidence, and surface a contract-miss signal rather than silently hiding it. The resulting target must still be moved to remote focus, checked against expected ID/label/contract, and activated with keyboard Enter. Do not add `locator.click()` or alter explicit transition delays.
  </action>
  <verify>
    <automated>node --check tests/lib/workflows.js</automated>
    <automated>node --check tests/lib/selector-validation.js</automated>
    <automated>npx playwright test tests/locator-contracts.spec.js tests/selector-contracts.spec.js</automated>
  </verify>
  <done>Locator success and fallback paths converge on the existing validated keyboard activation flow, with bounded diagnostics for contract misses and no mouse interaction.</done>
</task>

<task type="auto">
  <name>Add stable Locator and activation regression coverage</name>
  <files>tests/locator-contracts.spec.js</files>
  <action>
    Add synthetic DOM tests for a stable menu/settings container with duplicate labels, proving scoped Locator filtering selects the contract-backed target. Add a missing-contract test proving one evaluate fallback and diagnostic signal. Add a source/behavior regression that the target is remote-focused and Enter-activated through existing validation, and assert no `locator.click()` appears in the migrated helper path. Keep dynamic content fuzzy matching tests on the evaluator path.
  </action>
  <verify>
    <automated>node --check tests/locator-contracts.spec.js</automated>
    <automated>npx playwright test tests/locator-contracts.spec.js</automated>
  </verify>
  <done>Deterministic tests cover stable Locator filtering, bounded fallback, duplicate-label disambiguation, keyboard-only activation, and the absence of mouse clicks.</done>
</task>

</tasks>

<verification>
<automated>npx playwright test tests/locator-contracts.spec.js tests/dom-snapshot.spec.js tests/dom-scanning-performance.spec.js tests/selector-contracts.spec.js tests/readiness-pacing.spec.js</automated>
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Locator contract to external MyTV DOM | Stable selector contracts operate against mutable external markup and must fail diagnostically when assumptions drift. |
| Locator result to remote activation | A located element is not trusted for activation until existing focus and selector validation succeeds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Stable Locator contract mismatch | mitigate | Central contracts, one fallback with diagnostics, and duplicate-label synthetic coverage. |
| T-04-03-02 | Elevation of privilege | Locator-based activation | accept | No authentication/authorization behavior changes; preserve existing validated keyboard-only activation path. |
| T-04-03-SC | Tampering | Locator regression tests | mitigate | Run deterministic Locator and selector-contract specs. |
</threat_model>

</plan>
