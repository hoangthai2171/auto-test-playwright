---
phase: 04-optimize-dom-scanning-performance
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - tests/lib/batch-budget.js
  - tests/lib/workflows.js
  - tests/lib/ai-plan-runner.js
  - app/main.js
  - tests/batch-budget.spec.js
autonomous: true
requirements:
  - PERF-10

must_haves:
  truths:
    - "D-13: The implicit batch playback limit is 10 instead of 60, while explicit positive limits remain honored."
    - "D-13: Explicit itemLimit 0 means all available items subject to a hard runtime budget; omitted itemLimit uses the default 10."
    - "D-12: When the item or runtime budget is reached, the loop stops before starting another item and returns structured partial results rather than throwing solely for budget exhaustion."
    - "D-14: The runtime budget uses a named helper default with per-call options override and no new environment variables."
    - "D-15: Budget reports include completed/attempted counts, elapsed time, configured budget, and budgetLimited=true, and existing playback artifacts remain attached."
    - "AI plan validation and normalization preserve explicit zero semantics and remain backward-compatible with omitted itemLimit."
  artifacts:
    - path: tests/lib/batch-budget.js
      provides: "Named default/item-limit normalization and runtime budget policy"
      contains: "DEFAULT_BATCH_MAX_ITEMS"
    - path: tests/batch-budget.spec.js
      provides: "Deterministic item-limit, budget-stop, and report-shape coverage"
      contains: "budgetLimited"
    - path: app/main.js
      provides: "AI plan normalization that preserves explicit itemLimit 0"
      contains: "itemLimit"
  key_links:
    - from: tests/lib/workflows.js
      to: tests/lib/batch-budget.js
      via: "playAllItemsInFirstRow applies item and runtime budgets"
      pattern: "DEFAULT_BATCH_MAX_ITEMS|budgetLimited"
    - from: tests/lib/ai-plan-runner.js
      to: tests/lib/workflows.js
      via: "AI plan itemLimit and budget options reach batch playback"
      pattern: "itemLimit"
    - from: app/main.js
      to: tests/lib/ai-plan-runner.js
      via: "validated AI plan preserves explicit zero itemLimit semantics"
      pattern: "normalized.itemLimit"

---

<objective>
Reduce implicit batch playback work and enforce an observable runtime budget without turning intentional safety limits into functional failures.

Purpose: satisfy PERF-10 while preserving explicit caller intent, AI plan compatibility, playback artifacts, and the surrounding scenario's pass/fail ownership.
Output: reusable budget policy, integrated batch/AI behavior, and deterministic report coverage.
</objective>

<decision_coverage>
- D-12: Budget exhaustion stops the batch gracefully before another item and reports partial results.
- D-13: The default limit is 10; explicit zero means all available within the hard runtime budget.
- D-14: Runtime budget uses a named helper default with per-call options override and no environment variables.
- D-15: Budget-limited results include completed/attempted counts, elapsed time, budget, and budgetLimited=true; callers decide pass/fail.
</decision_coverage>

<execution_context>
@.planning/phases/04-optimize-dom-scanning-performance/04-CONTEXT.md
@.planning/phases/04-optimize-dom-scanning-performance/04-RESEARCH.md
@tests/lib/workflows.js
@tests/lib/ai-plan-runner.js
@app/main.js
@tests/lib/artifacts.js
</execution_context>

<tasks>

<task type="auto">
  <name>Implement shared batch budget policy</name>
  <files>tests/lib/batch-budget.js</files>
  <action>
    Add a focused CommonJS budget utility with a named default maximum of 10 items, explicit positive-limit handling, explicit zero-as-all semantics, elapsed-time checks, and a per-call options override for the runtime budget. Expose pure decisions that can be tested without live playback: whether another item may start, normalized limits, and structured budget metadata. Keep default duration selection in the helper policy rather than adding environment variables.
  </action>
  <verify>
    <automated>node --check tests/lib/batch-budget.js</automated>
    <automated>node -e 'const b=require("./tests/lib/batch-budget"); if(b.DEFAULT_BATCH_MAX_ITEMS!==10) process.exit(1); if(typeof b.createBatchBudget!=="function") process.exit(1);'</automated>
  </verify>
  <done>The shared policy distinguishes omitted, explicit positive, and explicit zero limits and exposes deterministic runtime-budget decisions.</done>
</task>

<task type="auto">
  <name>Integrate budget stopping, reports, and AI plan semantics</name>
  <files>tests/lib/workflows.js, tests/lib/ai-plan-runner.js, app/main.js</files>
  <action>
    Update `playAllItemsInFirstRow()` to use the shared default of 10, preserve explicit positive limits, treat explicit zero as all-within-budget, and check the budget before starting each next item. Stop gracefully when either limit is reached, set `budgetLimited: true`, and include completed/attempted counts, elapsed time, configured budget, and reason in the structured playback report while preserving existing item screenshots, player state, and batch artifact rendering. Thread any budget options through `ai-plan-runner.js`. Update AI plan normalization/validation in `app/main.js` so explicit `itemLimit: 0` is retained while omitted values remain omitted; do not change allowed actions or natural-language planner scope.
  </action>
  <verify>
    <automated>node --check tests/lib/workflows.js</automated>
    <automated>node --check tests/lib/ai-plan-runner.js</automated>
    <automated>node --check app/main.js</automated>
    <automated>npx playwright test tests/batch-budget.spec.js tests/ai-row-selection.spec.js tests/readiness-pacing.spec.js</automated>
  </verify>
  <done>Batch playback and AI plans enforce safe limits, stop before over-budget work, preserve explicit zero semantics, and return structured partial reports without throwing solely for budget exhaustion.</done>
</task>

<task type="auto">
  <name>Add deterministic budget and compatibility regression coverage</name>
  <files>tests/batch-budget.spec.js</files>
  <action>
    Add tests for omitted/default 10, explicit positive itemLimit, explicit zero, runtime budget reached before the next item, item limit reached, structured `budgetLimited` report fields, preserved item artifacts, and AI plan normalization retaining zero. Use fake clocks and injected playback callbacks/page fixtures so no live MyTV credentials or real playback are required. Add a source-contract assertion that the old implicit 60-item fallback is absent from the active batch path and that the surrounding scenario remains responsible for final failure decisions.
  </action>
  <verify>
    <automated>node --check tests/batch-budget.spec.js</automated>
    <automated>npx playwright test tests/batch-budget.spec.js</automated>
  </verify>
  <done>Deterministic tests prove item-limit compatibility, runtime stopping, report shape, artifact preservation, and AI zero semantics.</done>
</task>

</tasks>

<verification>
<automated>node --check tests/lib/batch-budget.js</automated>
<automated>node --check tests/lib/workflows.js</automated>
<automated>node --check tests/lib/ai-plan-runner.js</automated>
<automated>node --check app/main.js</automated>
<automated>npx playwright test tests/batch-budget.spec.js tests/locator-contracts.spec.js tests/dom-snapshot.spec.js tests/dom-scanning-performance.spec.js tests/ai-row-selection.spec.js tests/selector-contracts.spec.js tests/readiness-pacing.spec.js</automated>
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI plan input to batch policy | Local or cloud-generated plan values are untrusted configuration and must be normalized before playback starts. |
| Batch loop to external playback | Repeated playback can consume time and resources; the policy must stop before another item when a safety budget is exhausted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Denial of service | Unbounded batch playback | mitigate | Default 10-item limit, hard runtime budget, pre-item checks, and deterministic budget-stop tests. |
| T-04-04-02 | Tampering | AI plan itemLimit normalization | mitigate | Preserve explicit zero semantics, normalize invalid values, and test validated plan behavior. |
| T-04-04-03 | Information disclosure | Playback budget/report artifacts | accept | Reports retain existing item/player diagnostics needed by QA; no new sensitive data is introduced and existing artifact policy remains authoritative. |
| T-04-04-SC | Tampering | Budget regression tests | mitigate | Run syntax checks and deterministic budget/compatibility specs. |
</threat_model>

</plan>
