---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/lib/selectors.js
  - tests/lib/selector-validation.js
  - tests/selector-contracts.spec.js
autonomous: true
requirements:
  - SELECTOR-01
  - SELECTOR-02
  - SELECTOR-03
  - SELECTOR-04
must_haves:
  truths:
    - "D-01: Stable selector assumptions are centralized while dynamic helper-specific selectors remain explicitly allowed."
    - "D-02: Contracts are declarative, role-grouped, and carry identity, class, geometry, exclusion, and severity data."
    - "D-03: Ordered contract alternatives are evaluated and the matched alternative is reportable."
    - "D-04: Missing declared contracts fail with actionable diagnostics rather than undeclared broad matching."
    - "D-05: Diagnostics contain only the focused element and one top candidate."
    - "D-06: Diagnostics are captured before every activation attempt."
    - "D-07: Pre-activation diagnostics attach JSON and a screenshot."
    - "D-08: Fuzzy verification requires both a configured threshold and a clear score margin."
    - "D-09: Ambiguous highest-scoring candidates are rejected."
    - "D-10: Fallback/reselection attempts are bounded before terminal failure."
    - "D-11: Verification checks ID/attributes, normalized label, and focused-container relation with geometry as support."
    - "D-12: Health checks are ready-state preflight checks before scenario actions."
    - "D-13: Health checks cover the minimal focus, menu, content, and identity structure."
    - "D-14: Required absence fails and optional absence warns."
    - "D-15: Health results include status, contract, reason, and screenshot data."
  artifacts:
    - path: tests/lib/selectors.js
      provides: "Declarative role-based selector contracts and ordered alternatives"
      contains: "SELECTOR_CONTRACTS"
    - path: tests/lib/selector-validation.js
      provides: "Candidate verification, bounded diagnostics, and health-check primitives"
      contains: "runSelectorHealthCheck"
  key_links:
    - from: tests/lib/selector-validation.js
      to: tests/lib/selectors.js
      via: "contract lookup"
      pattern: "getSelectorContract"
---

<objective>
Create the declarative selector-contract registry and deterministic validation primitives that every later activation integration will use.

Purpose: establish one source of truth for stable selector assumptions, bounded candidate diagnostics, fuzzy verification thresholds, and required/optional preflight results before changing live workflow behavior.

Output: `selectors.js`, `selector-validation.js`, and synthetic Playwright coverage for contracts, diagnostics, verification, and health-check severity.
</objective>

<tasks>

<task type="execute">
  <name>Define declarative selector contracts</name>
  <files>tests/lib/selectors.js</files>
  <read_first>
    tests/lib/navigation.js
    tests/lib/content-rows.js
    tests/lib/workflows.js
    .planning/REQUIREMENTS.md
    .planning/phases/02-harden-selector-contracts-and-validation/02-CONTEXT.md
  </read_first>
  <action>Create a CommonJS `SELECTOR_CONTRACTS` registry grouped by `focus`, `leftMenu`, `contentContainer`, `contentItem`, `channel`, `menuItem`, `popup`, and `player`. Each contract must expose ordered alternatives plus `required`/`optional` severity, identity attribute names, class patterns, geometry bounds, and excluded ID prefixes. Export the registry and small lookup helpers such as `getSelectorContract(name)` without embedding page-specific matching logic in the config file. Keep contract values configurable and preserve the project’s two-space JavaScript style.</action>
  <acceptance_criteria>
    <criterion>`tests/lib/selectors.js` exports a declarative contract registry with all eight named roles and no Playwright `page` dependency.</criterion>
    <criterion>Every role contains ordered alternatives and explicit severity; geometry and identity assumptions are represented in the registry rather than duplicated as anonymous literals.</criterion>
    <criterion>Unknown contract lookup throws an actionable error naming the requested role.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Implement bounded selector validation and diagnostics</name>
  <files>tests/lib/selector-validation.js</files>
  <read_first>
    tests/lib/selectors.js
    tests/lib/navigation.js
    tests/lib/artifacts.js
    tests/lib/workflows.js
    .planning/phases/02-harden-selector-contracts-and-validation/02-CONTEXT.md
  </read_first>
  <action>Implement CommonJS helpers for collecting the focused element and one top candidate, evaluating ordered contract alternatives, checking fuzzy label threshold plus score margin, validating ID/attribute identity and focused-container containment, attaching JSON and a screenshot before activation, and running a ready-state health check. Expose a bounded fallback/reselection limit. Health results must distinguish `pass`, `fail`, and `warning`, fail required contracts, warn optional contracts, and return a structured summary with contract names and reasons. Reuse existing Vietnamese normalization and artifact attachment patterns instead of creating a second normalization implementation.</action>
  <acceptance_criteria>
    <criterion>Validation never returns an ambiguous match as valid solely because it has the highest score; threshold and score-margin checks are both enforced.</criterion>
    <criterion>Activation diagnostics contain the focused state and at most one top candidate, and the helper can attach JSON plus screenshot through the existing `testInfo.attach` pattern.</criterion>
    <criterion>Health-check output contains per-contract `pass`/`fail`/`warning` status and required failures are distinguishable from optional warnings.</criterion>
    <criterion>Fallback/reselection attempts are bounded by an explicit configurable limit and terminal failure includes the contract name and current focused state.</criterion>
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Add deterministic synthetic selector tests</name>
  <files>tests/selector-contracts.spec.js</files>
  <read_first>
    tests/ai-row-selection.spec.js
    tests/lib/selectors.js
    tests/lib/selector-validation.js
    .planning/codebase/TESTING.md
  </read_first>
  <action>Add a raw Playwright test spec using `page.setContent()` fixtures to cover ordered alternative selection, normalized Vietnamese labels, threshold and score-margin ambiguity rejection, focused/container identity, top-candidate-only diagnostics, bounded fallback failure, and required-versus-optional health outcomes. Keep the fixtures deterministic and independent of APP_URL, USERNAME, PASSWORD, CDP, and the shared live session.</action>
  <acceptance_criteria>
    <criterion>`npx playwright test tests/selector-contracts.spec.js` exits 0 with all selector-contract tests passing.</criterion>
    <criterion>A synthetic fixture with two near-equal candidates is rejected as ambiguous, while a clear threshold-and-margin match is accepted.</criterion>
    <criterion>Health tests prove required absence returns failure and optional absence returns warning without throwing the required failure.</criterion>
    <criterion>Diagnostics tests prove the serialized candidate collection contains no more than one candidate and includes focused state.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `npx playwright test tests/selector-contracts.spec.js` and `npx playwright test tests/ai-row-selection.spec.js`. Inspect the exported CommonJS API with Node to confirm the registry and validator modules load without a browser or environment credentials.
</verification>

<success_criteria>
- Selector assumptions are declarative and centrally discoverable.
- Validation has bounded, deterministic behavior for missing and ambiguous candidates.
- Diagnostics are minimal and attachable as JSON plus screenshot.
- Required and optional health-check outcomes are covered by synthetic tests.
</success_criteria>
