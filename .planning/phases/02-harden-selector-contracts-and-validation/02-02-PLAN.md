---
phase: 02
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/lib/navigation.js
  - tests/lib/content-rows.js
  - tests/lib/workflows.js
  - tests/lib/index.js
  - tests/lib/mytv-helpers.js
  - tests/lib/ai-plan-runner.js
  - tests/login-mytv.spec.js
  - tests/open-setting-mytv.spec.js
  - tests/play-channel-mytv.spec.js
  - tests/play-movie-mytv.spec.js
  - tests/search-content-mytv.spec.js
  - tests/selector-contracts.spec.js
autonomous: true
requirements:
  - SELECTOR-01
  - SELECTOR-02
  - SELECTOR-03
  - SELECTOR-04
must_haves:
  truths:
    - "D-01: Stable selector assumptions are centralized while dynamic helper-specific selectors remain explicitly allowed."
    - "D-03: Ordered contract alternatives are used by integrated navigation and activation paths."
    - "D-04: Unresolved selector matches fail with diagnostics instead of activating an undeclared candidate."
    - "D-06: Integrated activation captures state before every Enter attempt."
    - "D-08: Integrated activation requires fuzzy threshold and score-margin verification."
    - "D-09: Ambiguous matches trigger bounded reselection or failure."
    - "D-10: Activation retries use only declared fallbacks and a finite bound."
    - "D-11: Activation compares identity attributes, normalized labels, focus containment, and supporting geometry."
    - "D-12: Health checks run after app readiness and before scenario-specific actions."
    - "D-13: The preflight checks only minimal required focus/menu/content/identity structure."
    - "D-14: Required health failures stop scenarios while optional gaps warn."
    - "D-15: Health reports include structured status, reasons, checked contracts, and screenshot."
  artifacts:
    - path: tests/lib/navigation.js
      provides: "Contract-aware keyboard focus primitives"
      contains: "selectors"
    - path: tests/lib/content-rows.js
      provides: "Contract-aware content discovery and activation boundary"
      contains: "selector-validation"
    - path: tests/lib/workflows.js
      provides: "Verified user-facing activation and ready-state preflight integration"
      contains: "runSelectorHealthCheck"
  key_links:
    - from: tests/lib/workflows.js
      to: tests/lib/selector-validation.js
      via: "verified activation and health-check calls"
      pattern: "verifyFocusedTarget|runSelectorHealthCheck"
    - from: tests/lib/mytv-helpers.js
      to: tests/lib/index.js
      via: "backward-compatible CommonJS alias"
      pattern: "require.*index"
---

<objective>
Integrate contract validation into the shared MyTV navigation and activation paths, and run the ready-state selector health check before scenario-specific actions without breaking existing imports.

Purpose: make real channel, movie, search, menu, and content activation failures actionable while retaining keyboard-only behavior, fuzzy Vietnamese matching, shared session ordering, and the Phase 1 compatibility surface.

Output: all user-facing activation paths use bounded pre-Enter verification and diagnostics; the post-login ready state produces a structured selector health result.
</objective>

<tasks>

<task type="execute">
  <name>Wire contracts into focus and content discovery</name>
  <files>tests/lib/navigation.js, tests/lib/content-rows.js</files>
  <read_first>
    tests/lib/navigation.js
    tests/lib/content-rows.js
    tests/lib/selectors.js
    tests/lib/selector-validation.js
    tests/ai-row-selection.spec.js
  </read_first>
  <action>Replace duplicated stable attribute/class/geometry assumptions in navigation and content-row candidate paths with lookups from `selectors.js`, while preserving dynamic helper-specific selectors under the two-tier rule. Keep `remoteFocusById`, `remoteFocusByText`, `collectVisibleContentRows`, and `focusRequestedContentRow` signatures and return shapes unchanged. Route user-facing content/control activation through the validator’s bounded pre-Enter verification; keep virtual-keyboard character entry keyboard-driven and compatible with its existing ID navigation.</action>
  <acceptance_criteria>
    <criterion>Navigation and content-row helpers load `selectors.js` and no longer duplicate the centralized stable contract values for their covered roles.</criterion>
    <criterion>Existing public helper signatures and focused-container behavior remain callable by `workflows.js` and `tests/ai-row-selection.spec.js`.</criterion>
    <criterion>Every content/control Enter activation has a pre-activation validation/diagnostics boundary; virtual keyboard input still enters characters one key at a time.</criterion>
    <criterion>Current geometry and Vietnamese normalization behavior remains covered by existing deterministic tests.</criterion>
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Integrate verified activation and ready-state health check</name>
  <files>tests/lib/workflows.js, tests/lib/index.js, tests/lib/mytv-helpers.js</files>
  <read_first>
    tests/lib/workflows.js
    tests/lib/index.js
    tests/lib/mytv-helpers.js
    tests/lib/selector-validation.js
    tests/lib/artifacts.js
    tests/fixtures/mytv-session-fixture.js
    tests/login-mytv.spec.js
    tests/open-setting-mytv.spec.js
    tests/play-channel-mytv.spec.js
    tests/play-movie-mytv.spec.js
    tests/search-content-mytv.spec.js
    tests/lib/ai-plan-runner.js
    .planning/phases/02-harden-selector-contracts-and-validation/02-CONTEXT.md
  </read_first>
  <action>Use one shared verified activation helper at the workflow call sites for login controls, profile/service/menu selection, channel/movie/search-result activation, and content playback. Pass the intended role plus expected ID/label where available, capture JSON and screenshot before Enter, try only declared fallbacks with the configured bound, and fail on unresolved ambiguity. Invoke the minimal health check after the existing home-ready/popup-close flow and before scenario-specific actions; preserve optional parameters and existing `mytv-helpers.js` imports. Re-export any public validation/health APIs through `tests/lib/index.js` only when callers need them.</action>
  <acceptance_criteria>
    <criterion>Workflow activation paths verify the focused target immediately before Enter and do not silently continue after an ambiguous or failed verification.</criterion>
    <criterion>Health check runs after app readiness rather than at `DOMContentLoaded`, and its required failure prevents scenario actions while optional absence is reported as warning.</criterion>
    <criterion>Existing specs can still `require("./lib/mytv-helpers")` and receive the prior workflow exports without import-path changes.</criterion>
    <criterion>Activation diagnostics attach JSON and screenshot with a stable artifact label before each activation attempt.</criterion>
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Extend regression coverage for integrated validation</name>
  <files>tests/selector-contracts.spec.js</files>
  <read_first>
    tests/selector-contracts.spec.js
    tests/lib/navigation.js
    tests/lib/content-rows.js
    tests/lib/workflows.js
    tests/lib/index.js
    tests/ai-row-selection.spec.js
  </read_first>
  <action>Extend synthetic fixtures to exercise the integrated navigation/content/workflow activation boundary: assert a clear target reaches the activation call, an ambiguous target is reselected or fails within the configured bound, pre-activation diagnostics are attached, and the health summary is produced after a ready-state fixture. Add compatibility assertions for the `mytv-helpers.js` import surface.</action>
  <acceptance_criteria>
    <criterion>`npx playwright test tests/selector-contracts.spec.js tests/ai-row-selection.spec.js` exits 0.</criterion>
    <criterion>Regression tests assert that ambiguous activation does not press Enter on an unverified candidate.</criterion>
    <criterion>Regression tests assert that the compatibility alias still exposes representative workflow and validation exports.</criterion>
    <criterion>When live environment variables are available, `npx playwright test` runs with the existing one-worker configuration and no mouse-based interaction is introduced.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `npx playwright test tests/selector-contracts.spec.js tests/ai-row-selection.spec.js`, then run the normal suite with `npx playwright test` when staging credentials are available. Confirm `workers: 1` remains unchanged, inspect the HTML report for JSON/screenshot activation artifacts, and verify all existing specs still import through `tests/lib/mytv-helpers.js`.
</verification>

<success_criteria>
- All covered workflow activations verify identity before Enter and fail safely on ambiguity.
- Selector drift produces minimal, actionable JSON and screenshot diagnostics.
- Ready-state health check distinguishes required failures from optional warnings.
- Existing module exports, keyboard-only navigation, fuzzy Vietnamese matching, and shared-session behavior remain intact.
</success_criteria>
